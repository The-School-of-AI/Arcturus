# Scripts

- `test_all.sh` is the canonical runner for backend/frontend CI gates.
- `manual_diagnostics.sh` is the canonical runner for curated manual diagnostics.
- `test_*.py` files here are utility/manual verification scripts and are not part of pytest discovery.
